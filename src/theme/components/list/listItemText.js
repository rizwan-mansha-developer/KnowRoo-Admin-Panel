import { fontSize } from '@mui/system';

const listItemText = {
  styleOverrides: {
    root: {
      marginTop: 0,
      marginBottom: 0,
      fontWeight: 'bold',
    },
  },
};

export default listItemText;
